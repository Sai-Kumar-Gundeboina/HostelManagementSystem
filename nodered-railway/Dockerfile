# Use the official Node-RED base image
FROM nodered/node-red:latest

# Optional: install extra nodes here
# Example: install MySQL and JWT contrib nodes
# RUN npm install node-red-node-mysql node-red-contrib-auth node-red-contrib-jwt

# Optional: copy your custom flows into the container
# COPY flows.json /data/flows.json

# Expose the default Node-RED port
EXPOSE 1880

# The container starts Node-RED automatically (from base image)
